# ============================================================
# BotOS Core — SDK (L4: Development Layer)
# ============================================================
# Aggregates libbot (core utilities) and BotUI (GUI).
# ============================================================

add_subdirectory(libbot)
add_subdirectory(botui)
